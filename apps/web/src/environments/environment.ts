/**
 * Trial recovery URL is not linked anywhere in the app.
 * Change `trialMgmtPath` per deployment; optional production file replacement.
 */
export const environment = {
  production: false,
  trialMgmtPath: 'svc-vq8m-k3nz7xp2',
};
